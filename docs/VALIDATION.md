# 基础版验收记录

验收日期：2026-09-12。未配置个人模型密钥或 X API Token。

- `npm test`：8 项核心测试通过，覆盖中文时间识别、周/月重复、重复结束日期、DDL 与提醒、备份校验、ICS 导入导出及重复排除。
- `npm run build`：TypeScript 校验与 Vite 生产构建通过。
- 本机浏览器：验证中文快速添加、完成并生成下一周、撤销、清理测试任务；计划日期修改后 DDL 保持不变。
- WebMCP：验证任务查询、新建、完成；非法日期、空标题、未知任务 ID 正确报错。
- 布局：检查 1440px 桌面与 390px 窄屏；待办、日历、GPU、设置页未出现横向溢出。
- 助手：验证跨页面保留对话、未配置密钥时的使用指导。未调用真实付费模型。
- GPU：本机服务使用内存登录会话完成只读查询，页面成功显示真实节点与插槽状态；不提供真实资源提交或释放。
- 公开内容：RSDL 目录 16 条；GitHub 构建中 X 官方公开嵌入成功返回 3 条选定帖节选。目录链接不含正文，X 快照不代表完整时间线。
- 部署：GitHub Actions 构建和 Pages 部署成功；正式 HTML、JS、CSS 返回成功，发布脚本和样式的 SHA-256 与本地构建一致。
- 本机服务正式域名的跨域预检返回 HTTP 204 和正确允许来源。浏览器是否允许 HTTPS 站点连接本机网络，仍取决于浏览器权限。

公开仓库不包含实验室地址、账号、密码、会话、API Key 或真实 GPU 响应。测试凭据仅在进程/页面内存中使用，私人调试目录被 Git 忽略。

## 周报功能增量验收

验收日期：2026-09-12。

- `npm test`：16 项通过。新增覆盖完成时间的本机时区边界、跨年周次、所选上下文隔离、DDL 原条目保留、缺正文保护、异常模型结构、旧备份迁移及周报备份往返。
- `npm run build`：TypeScript 校验与 Vite 构建通过，周报组件和 Markdown 预览按页面懒加载。
- 写作 Skill：`quick_validate.py` 校验通过，个人 skills 目录与网站共用相同规则，不包含原始周报。
- 浏览器：已验证真实公众号目录文章加入周报、补充阅读笔记、完成 DDL 后选入本周、本地整理、Markdown 编辑、清空后继续输入、切周隔离和刷新保留修改。
- 1440px 桌面与 390px 手机尺寸无横向溢出；检查深色模式与 Skill 阅读弹窗。
- 共享接口：使用仅监听本机的测试服务验证请求携带写作规则、只选中的 1 篇材料和 1 项完成 DDL，输出上限 8,192 tokens；成功返回时组装稿件，异常结构不覆盖原稿，取消请求保留已有内容。没有调用真实付费模型。
- 下载：Skill 下载返回浏览器成功事件。周报提供指向 UTF-8 Markdown Blob 的直接下载链接及正确 `.md` 文件名；内置浏览器未返回该动态文件的下载事件，尚未确认其最终落盘。另提供复制 Markdown 与手动编辑保存入口。

测试文章笔记、模拟接口和本机调试记录均未打包发布。

## 公众号更新修复

验收日期：2026-09-12。

- 原因：原采集器仅取资讯栏前 16 条；无本机服务时按钮重读部署快照，界面未区分检查时间与数据时间。
- 实际验证 RSDL 公开 `/api/links.php?action=list` 支持跨域读取，返回 1,067 条记录；完整目录目前最新收录时间为 2026-08-12，上游目录滞后仍会明确提示。
- 浏览器真实直连成功，无需本机服务；旧 16 条目录升级时新增 1,051 条，重复检查新增 0 条。已有旧版重复记录保持原数据，新的同步按微信文章身份匹配，不继续制造重复。
- 变化检测关键词可搜到 56 条目录记录，首屏 50 条，加载更多后为 56 条；全文搜索范围不受分页限制。
- 默认进入公众号页检查及每 5 分钟轮询，可选每分钟；后台暂停，返回页面时补查。已验证间隔选择、检查状态和完整目录加载。
- 23 项测试覆盖完整目录、不使用缓存的实时请求、失败后本机/部署缓存回退、取消请求、老文章 ID/手动正文/已读/收藏/周报引用保留，以及旧设置升级。
