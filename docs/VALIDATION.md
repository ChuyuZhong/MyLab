# 基础版验收记录

验收日期：2026-09-12。未配置个人模型密钥或 X API Token。

- `npm test`：8 项核心测试通过，覆盖中文时间识别、周/月重复、重复结束日期、DDL 与提醒、备份校验、ICS 导入导出及重复排除。
- `npm run build`：TypeScript 校验与 Vite 生产构建通过。
- 本机浏览器：验证中文快速添加、完成并生成下一周、撤销、清理测试任务；计划日期修改后 DDL 保持不变。
- WebMCP：验证任务查询、新建、完成；非法日期、空标题、未知任务 ID 正确报错。
- 布局：检查 1440px 桌面与 390px 窄屏；待办、日历、GPU、设置页未出现横向溢出。
- 助手：验证跨页面保留对话、未配置密钥时的使用指导。未调用真实付费模型。
- GPU：本机服务使用内存登录会话完成只读查询，页面成功显示真实节点与插槽状态；不提供真实资源提交或释放。
- 公开内容：RSDL 目录 16 条；GitHub 构建中 X 官方公开嵌入成功返回 3 条选定帖节选。目录链接不含正文，X 快照不代表完整时间线。
- 部署：GitHub Actions 构建和 Pages 部署成功；正式 HTML、JS、CSS 返回成功，发布脚本和样式的 SHA-256 与本地构建一致。
- 本机服务正式域名的跨域预检返回 HTTP 204 和正确允许来源。浏览器是否允许 HTTPS 站点连接本机网络，仍取决于浏览器权限。

公开仓库不包含实验室地址、账号、密码、会话、API Key 或真实 GPU 响应。测试凭据仅在进程/页面内存中使用，私人调试目录被 Git 忽略。
