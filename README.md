# MyLab · 个人科研工作台

GitHub Pages: https://chuyuzhong.github.io/MyLab/

一个以个人待办为首页的科研工作台。React + TypeScript + Vite，静态部署，不需要把私人数据放进 GitHub。

## 已实现

- 待办新增、编辑、完成、删除、优先级、项目、备注、中文快速添加。
- 单次 / 每天 / 工作日 / 每周指定几天 / 每月重复；完成后生成下一次，保留完成记录。独立计划日期、DDL 和提醒时间。
- 月历、日程与任务联合展示；导入 ICS（含重复、排除日期、时区定义），导出待办到系统日历。
- X 与公众号两个独立页面；自定义来源、RSS/Atom/JSON、关键词搜索、未读/收藏、文章转待办、补充正文。
- GPU 节点与插槽真实只读查询；个人申请草稿、使用记录和文本导出。没有替代原系统提交申请或释放资源。
- 全站助手，个人兼容 Chat Completions 的接口，DeepSeek 默认配置，自定义翻译提示词、限定上下文、取消生成。
- [写周报](https://chuyuzhong.github.io/MyLab/#/weekly)：选择已读公众号论文、本周实际完成的 DDL，补充工作、计划和 idea；按个人写作 Skill 生成、编辑、预览并下载 Markdown，草稿按周保存。
- 本机持久化、备份合并导入、深色模式、移动端布局、键盘快捷键（Ctrl K 搜索、Ctrl / 助手）。

## 启动

需要 Node.js 24。

```sh
npm ci
npm run dev
```

浏览器打开终端显示的地址。生产构建：`npm run build`；逻辑验证：`npm test`。

## 真实数据与已知边界

### RSDL / 遥感与深度学习

预设 RSDL 使用配套网站公开完整接口 `https://rsdl.info/api/links.php?action=list`，包含期刊、会议、论文赏读、预印本、资讯等全部栏目。接口支持 CORS，GitHub Pages 页面可以直接读取，无需本机服务或 API Key。2026-09-12 验证返回 1,067 条有效记录，最新收录时间为 2026-08-12；上游尚未收录的微信文章无法从这个目录得到。

进入“公众号阅读”立即检查，默认每 5 分钟检查一次，可在页面选择每分钟、其他间隔或仅进入页面/手动。后台暂停请求，返回后补查；更新只在网站运行期间进行。请求不使用浏览器缓存，完整读取目录，不再仅取资讯栏前 16 条。列表每次展示 50 条，可加载更多，搜索覆盖所有本机文章。

实时接口失败时，尝试已配对的本机服务，再退回网站部署缓存；全部失败则保留本机文章。页面分别显示实时/本机/缓存来源、本次新增数量、检查时间、缓存采集时间和目录最新收录时间，不把重读旧快照当成新内容。构建及每六小时排程更新完整目录作为备用缓存，排程可能延迟；该排程不限制正常的浏览器实时读取。

目录的 `added_at` 是收录时间，不等于微信发布时间。目录不含全文；配对新版本机服务后，打开文章可尝试提取微信公开文字正文，也可点击“获取正文”。微信可能返回验证页，此时明确提示打开原文、粘贴正文或接入全文 RSS，不把验证提示当成文章。文字阅读不包含图片、视频和评论，也不能绕过微信验证。

实时连接公开目录不等于微信推送：目录可能滞后，超过七天未收录新条目会在页面提示。自定义公众号需提供可读取的 RSS/Atom/JSON 地址，仅填写公众号名称不能自动抓取其发布历史。对现有微信文章按稳定链接身份合并更新，保留既有 ID、已读、收藏、已获取的正文及周报引用。

持续获取微信端新文章，可自行运行 [WeRSS（we-mp-rss）](https://github.com/rachelos/we-mp-rss/blob/main/README.zh-CN.md)，在该服务中完成所需授权并添加公众号，开启全文输出，将它实际生成的 RSS 地址粘贴到 MyLab 的“编辑订阅 → RSS 地址”。设置中选择“通过本机服务”，并完成配对。本机服务支持明确允许的 `127.0.0.1:8001`、`127.0.0.1:4000` 的 `/feed`、`/feeds` 路径；其他端口需在私有 `.env.local` 中配置 `MYLAB_LOCAL_FEED_ORIGINS` 后重启。扫码、登录状态和采集任务由该服务维护，MyLab 不收集微信登录凭据；更新速度取决于采集间隔、授权有效期及微信响应，不能保证即时到达。该采集服务尚未代用户安装或授权。

每张公众号卡片可切换已读。标记后点击“AI 阅读并加入周报”，使用设置中的共享模型 API 对已有正文整理笔记，保存后选入本周周报。只有链接时先尝试获取正文，失败不会调用模型编造内容。已有个人笔记与手工修改的周报原稿保持不变；周报页重新“本地整理”或“按我的写法生成”才合成新稿。AI 笔记标注为待核对。订阅编辑支持七种来源配色，方形图标和文章卡片底纹同步生效。

### X / Tibo

预设 @thsottiaux。GitHub 构建环境已于 2026-09-12 成功通过 X 官方公开嵌入接口取得 3 条选定帖的短节选，逐条保留来源和原帖地址。每次构建会尝试重新获取这些帖子，失败则保留上次快照；本机直连可能受网络限制。这些是选定帖快照，不会自动发现账号的新帖子。

持续更新：在设置填写自己的 X API Bearer Token，并连接本机服务；或在订阅设置里填写可用 RSS / Atom / JSON 地址。官方 API 读取最近 10 条（不含转帖），未实现完整历史分页，长时间离线可能有遗漏。个人 API 可能产生平台费用。

本机配置：首次使用将 `.env.example` 复制为 `.env.local`，已有文件则只更新对应变量。`MYLAB_PROXY_URL=http://127.0.0.1:7897` 指定 X 出站代理，运行 `npm run bridge` 自动加载，修改后需重启服务。7897 是网络代理端口，网站设置中的本机服务地址仍为 `http://127.0.0.1:4318`。此代理只用于 X API，实验室 GPU 与微信正文保持直连。

更新 Tibo：启动本机服务 → 网站设置填写服务配对码、个人 X Bearer Token → X 动态订阅用户名填 `thsottiaux` → 点击“检查更新”。也可在设置开启 X 每 15 / 30 / 60 分钟检查，页面可见且服务运行时生效。若填写了 RSS 地址会优先读取 RSS。代理不能替代 X API 账号、Token 与接口权限，详见 [X 官方时间线教程](https://docs.x.com/tutorials/explore-a-users-posts)。

### 实验室 GPU

支持 Determined 的 `/api/v1/auth/login`、`/api/v1/agents`、`/api/v1/resource-pools` 和 `/api/v1/master`。账号、密码、系统地址由用户在本机填写，不包含在仓库。GPU 状态不随网站公开部署。

```sh
npm run bridge
```

1. 在网站“设置与数据”填写连接服务地址（默认 `http://127.0.0.1:4318`）和终端显示的配对码。
2. 在“GPU 资源 → 连接 / 登录”填写系统根地址、账号和密码。
3. 登录成功后读取节点状态。本机需要能访问实验室网络或 VPN。

连接服务仅监听 127.0.0.1，校验 Host、允许来源和随机配对码。会话、密码、X Token 不写入文件。关闭连接服务后会话失效；刷新网页后需要重新填写配对码（配对码不会写入浏览器存储）。如果浏览器询问本地网络访问，应由用户允许。部分浏览器可能不支持从 HTTPS 页面访问本机服务，可在本机开发地址运行完整界面。

默认允许生产站点和本机开发地址。自定义域名可通过 `MYLAB_ALLOWED_ORIGINS` 环境变量追加。RSS 代理默认只读 HTTPS 公网地址，并拒绝私有地址及相关重定向；可通过 `MYLAB_LOCAL_FEED_ORIGINS` 明确允许本机采集服务的订阅路径，本机订阅不跟随重定向。微信正文接口只接受微信公开文章路径，并校验每次重定向。

资源状态是调度分配状态，不是实时利用率；当前接口没有提供显存占用。个人草稿和“使用中/已结束”都是手动记录，不代表原系统真实操作。请通过“原管理系统”执行申请或释放资源。

### 模型接口

在设置中填写 Base URL、模型名和个人 API Key；可以先点击“测试模型连接”。默认 DeepSeek，模型名可按服务实际支持情况修改。调用直接从浏览器发送到所填接口，因此该接口必须支持跨域。密钥仅保留在当前页面内存，不写入仓库、localStorage 或备份。

助手仅在用户发送时请求模型，发送前可勾选当前文章或今天待办作为上下文；文章页的“AI 阅读并加入周报”和周报生成按钮也会使用同一接口。助手没有执行 GPU 写操作或自主修改任务的工具。未填写密钥时显示本地使用指导，不伪造模型结果。

### 个人科研周报

在公众号文章阅读页点击“加入本周周报”，或者在“写周报”中选择已读、收藏或其他文章。可补充论文原题、出版信息和个人阅读笔记。完成事项按所选周的实际完成日期筛选，默认只列带 DDL 的任务，可另外勾选普通已完成待办。

“本地整理”把笔记和事项排成周报；“按我的写法生成”与助手共用模型接口，按 Skill 撰写论文评述、整理其他栏目。生成结果可直接编辑，下载为 `.md`。只有链接的文章不会被编成已读论文。详见 [周报使用与写法](docs/WEEKLY_REPORTS.md)。

可复用规则：[SKILL.md](public/skills/chuyu-weekly-report/SKILL.md)。发布版本可在页面“我的写作 Skill”中阅读和下载。仓库只保存提炼后的写作规则，原始个人周报没有上传。

### 日历、提醒与存储

- ICS 导入范围：过去 31 天至未来 366 天，重复展开上限 5000 项。同名日历重新导入时更新对应日程；不提供账号实时双向同步。
- 页面运行时每 30 秒检查提醒；浏览器后台节流、休眠或关闭页面可能延迟。重新打开会提示错过的提醒。重要 DDL 请导出至系统日历。
- localStorage 按浏览器和网站来源隔离；开发站与线上站数据互不共享。清理浏览器数据会删除记录，建议定期备份。
- 普通备份去除连接服务地址配置中的私人实验室地址、订阅接口地址、全部密钥；正文/备注/原文链接仍是个人数据。原始存储导出用于故障恢复，包含本机配置，勿公开上传。

## 发布

`.github/workflows/pages.yml` 在 main 提交、手动触发及排程时构建并部署。仓库 Settings → Pages 的构建来源应为 GitHub Actions。所有页面采用 hash 路由，支持 GitHub Pages 子目录以及直接刷新。

`.local/`、`.env*`、`node_modules/`、`dist/` 被 Git 忽略。部署只包含静态 `dist/`，本机连接服务不会部署到 Pages。请勿把 API Key 写进 `VITE_*` 环境变量，因为它们会被编译进网页。

## 验证范围

核心逻辑测试覆盖中文时间识别、重复任务、月底跨月、重复结束日期、提醒顺延、DDL 独立性、备份校验、日历往返和重复排除日期。页面还提供可选 WebMCP 的任务读取、新建和完成操作，浏览器不支持时普通交互保持可用。
